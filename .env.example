# Copy to .env and fill in real values. .env is git-ignored (never committed).
# Generate APP_KEY (base64 of 32 random bytes):
#   php -r "echo base64_encode(random_bytes(32)), PHP_EOL;"

APP_KEY=

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=medic_vault_db
DB_USER=medivault_app
DB_PASS=
